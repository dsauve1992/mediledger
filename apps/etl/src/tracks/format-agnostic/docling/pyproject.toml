[project]
name = "mediledger-docling"
version = "0.1.0"
description = "Experimental Docling ingester for RAMQ documents — A/B baseline against ETL steps 1-4"
requires-python = ">=3.12,<3.13"
dependencies = ["docling"]
